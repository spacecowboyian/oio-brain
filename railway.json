{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "python scripts/slackbot_social_media.py",
    "restartPolicyType": "always",
    "restartPolicyMaxRetries": 5
  }
}
